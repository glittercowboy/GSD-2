---
phase: 04-remote-access-settings-ui
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/web/remote-access-api.test.ts
  - src/web/remote-access-panel.test.ts
  - src/web/tailscale-status.test.ts
  - src/web/tailscale-setup.test.ts
autonomous: true
requirements: [SETT-01, SETT-02, SETT-03, SETT-04, TAIL-09, TAIL-10, TAIL-11]

must_haves:
  truths:
    - "All four test stub files exist and can be run with npx tsx --test without crashing"
    - "Each test file uses test.todo() for placeholder tests — no silently passing stubs"
  artifacts:
    - path: "src/web/remote-access-api.test.ts"
      provides: "Test stubs for password change API and Tailscale status API"
      contains: "describe.*password"
    - path: "src/web/remote-access-panel.test.ts"
      provides: "Test stubs for RemoteAccessPanel UI behaviors"
      contains: "describe.*remote"
    - path: "src/web/tailscale-status.test.ts"
      provides: "Test stubs for Tailscale status parsing and DNS name handling"
      contains: "describe.*status"
    - path: "src/web/tailscale-setup.test.ts"
      provides: "Test stubs for setup assistant logic (platform detect, install command, auth URL)"
      contains: "describe.*setup"
  key_links: []
---

<objective>
Create Wave 0 test stub files for all Phase 4 test targets. Stubs use `test.todo()` to declare intended behaviors without silently passing, so later plans MUST replace them with real assertions.

Purpose: Plans 01, 02, and 03 reference these test files in their `<verify>` commands. The stubs must exist before any Wave 1 task runs so automated verification works from the first task.
Output: Four test stub files with `test.todo()` placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-remote-access-settings-ui/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create four test stub files using test.todo() placeholders</name>
  <files>src/web/remote-access-api.test.ts, src/web/remote-access-panel.test.ts, src/web/tailscale-status.test.ts, src/web/tailscale-setup.test.ts</files>
  <read_first>
    - .planning/phases/04-remote-access-settings-ui/04-RESEARCH.md (Validation Architecture section — lists the four files and their coverage targets)
  </read_first>
  <action>
    Create four test stub files using `node:test`. Each file should import `describe`, `it` from `node:test`. Use `it.todo('description')` for ALL placeholder tests — NOT `assert.ok(true)`. This ensures stubs are visible as unimplemented rather than silently green. Each stub MUST have a `// TODO: Replace with real assertions in Plan 04-{NN}` comment indicating which plan fills it.

    **1. `src/web/remote-access-api.test.ts`** (covers SETT-01, SETT-02):
    ```typescript
    import { describe, it } from 'node:test'

    // TODO: Replace stubs with real assertions in Plan 04-01
    describe('Password change API (/api/settings/password)', () => {
      it.todo('should hash and store new password via setPassword()')
      it.todo('should reject passwords shorter than 4 chars with 400')
      it.todo('should rotate session secret after password change')
      it.todo('should re-issue session cookie for current browser (D-07)')
    })

    describe('Tailscale status API (/api/tailscale/status)', () => {
      it.todo('should return connected status with hostname and URL')
      it.todo('should return connected: false when Tailscale is not running')
      it.todo('should return installed: false when Tailscale CLI is missing')
    })
    ```

    **2. `src/web/remote-access-panel.test.ts`** (covers SETT-03, SETT-04):
    ```typescript
    import { describe, it } from 'node:test'

    // TODO: Replace stubs with real assertions in Plan 04-03
    describe('RemoteAccessPanel', () => {
      it.todo('should render password form with min 4 char validation')
      it.todo('should display Tailscale URL as copyable link when connected')
      it.todo('should show password-required notice when no password configured')
      it.todo('should launch setup assistant on button click')
      it.todo('should show connect/disconnect toggle when installed and password set')
    })
    ```

    **3. `src/web/tailscale-status.test.ts`** (covers SETT-02, TAIL-11, SETT-04):
    ```typescript
    import { describe, it } from 'node:test'

    // TODO: Replace stubs with real assertions in Plan 04-01
    describe('Tailscale status parsing', () => {
      it.todo('should strip trailing dot from DNSName')
      it.todo('should build valid HTTPS URL from hostname')
      it.todo('should return null when tailscale is not connected')
      it.todo('should parse hostname from status JSON')
    })
    ```

    **4. `src/web/tailscale-setup.test.ts`** (covers SETT-03, TAIL-09, TAIL-10):
    ```typescript
    import { describe, it } from 'node:test'

    // TODO: Replace stubs with real assertions in Plan 04-02
    describe('Tailscale setup assistant', () => {
      it.todo('should detect platform via process.platform')
      it.todo('should return brew install command for darwin')
      it.todo('should return curl script command for linux')
      it.todo('should return error for unsupported platform')
      it.todo('should parse auth URL from tailscale up stderr')
      it.todo('should report success when tailscale up exits 0')
      it.todo('should handle disconnect step running tailscale down')
    })
    ```

    All tests are `it.todo()` stubs — they will NOT pass as green. Plans 01, 02, and 03 MUST replace the relevant `.todo()` calls with real assertions when implementing the production code.
  </action>
  <verify>
    <automated>npx tsx --test src/web/remote-access-api.test.ts src/web/remote-access-panel.test.ts src/web/tailscale-status.test.ts src/web/tailscale-setup.test.ts 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/web/remote-access-api.test.ts contains `it.todo(`
    - src/web/remote-access-api.test.ts contains `describe('Password change API`
    - src/web/remote-access-api.test.ts contains `describe('Tailscale status API`
    - src/web/remote-access-api.test.ts does NOT contain `assert.ok(true`
    - src/web/remote-access-panel.test.ts contains `it.todo(`
    - src/web/tailscale-status.test.ts contains `it.todo(`
    - src/web/tailscale-setup.test.ts contains `it.todo(`
    - src/web/tailscale-setup.test.ts contains `unsupported platform`
    - src/web/tailscale-setup.test.ts contains `disconnect`
    - All four files run without crash via `npx tsx --test`
  </acceptance_criteria>
  <done>All four test stub files exist with test.todo() placeholders that are visible as unimplemented. No silently passing stubs. Each file has a TODO comment pointing to the plan that fills it.</done>
</task>

</tasks>

<verification>
- `ls src/web/remote-access-api.test.ts src/web/remote-access-panel.test.ts src/web/tailscale-status.test.ts src/web/tailscale-setup.test.ts` lists all four files
- `npx tsx --test src/web/remote-access-api.test.ts src/web/remote-access-panel.test.ts src/web/tailscale-status.test.ts src/web/tailscale-setup.test.ts` exits 0 (todo tests are not failures)
- `grep -c "it.todo" src/web/remote-access-api.test.ts` returns at least 7
</verification>

<success_criteria>
- Four test stub files exist at the declared paths
- All stubs use test.todo() — zero silently-passing placeholder assertions
- Each stub contains describe/it blocks for the requirements it covers
- All stubs run without crash
</success_criteria>

<output>
After completion, create `.planning/phases/04-remote-access-settings-ui/04-00-SUMMARY.md`
</output>
